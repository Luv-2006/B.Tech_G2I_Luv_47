#include<stdio.h>
#include<math.h>
int main()
{
    int n,r,c=0;
    printf("Enter the number  ");
    scanf("%d",&n);
    
    while(n>0)
    {
        r=n%10;
        c=c*10+r;
        n=n/10;
        
    }
    printf("%d",c);
    return 0;
}
